import { Module } from "../Model/Config";
export default class AwsValidator {
    static validate(flows: Array<Array<Module>>): string[] {
        for (let flow of flows) {
            for (let m of flow) {
                
            }
            
        }
        return [];
    }
}